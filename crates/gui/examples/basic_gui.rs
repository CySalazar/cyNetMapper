//! Basic GUI example for cyNetMapper
//! 
//! This example demonstrates the basic functionality of the cyNetMapper GUI.

use cynetmapper_gui::*;

fn main() {
    println!("Basic GUI example - this would launch the Tauri application");
    // In a real implementation, this would initialize and run the Tauri app
    // tauri::Builder::default()
    //     .run(tauri::generate_context!())
    //     .expect("error while running tauri application");
}