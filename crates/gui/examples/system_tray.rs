//! System tray example for cyNetMapper
//! 
//! This example demonstrates the system tray functionality.

use cynetmapper_gui::*;

fn main() {
    println!("System tray example - this would launch the Tauri application with system tray");
    // In a real implementation, this would initialize and run the Tauri app with system tray
    // tauri::Builder::default()
    //     .system_tray(tauri::SystemTray::new())
    //     .run(tauri::generate_context!())
    //     .expect("error while running tauri application");
}